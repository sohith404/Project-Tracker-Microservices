server:
  port: 8761

spring:
  application:
    name: eureka-server

eureka:
  client:
    # Since this is the server, it shouldn't try to register with a server
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:8761/eureka/